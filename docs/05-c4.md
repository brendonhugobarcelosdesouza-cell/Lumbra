# 05 — Diagramas C4

## Nível 1 — Contexto

```mermaid
C4Context
    title Lumbra — Contexto
    Person(user, "Usuário", "Pessoa que centraliza sua vida digital no Lumbra")
    Person(contact, "Contato de confiança", "Recebe alertas escalonados (opt-in)")
    System(lumbra, "Lumbra", "Personal Intelligence Platform: memória, busca, agentes, automação")
    System_Ext(ai, "Provedores de IA", "OpenAI, Anthropic, Gemini (Ollama é local)")
    System_Ext(cloudfs, "Armazenamento em nuvem", "Google Drive, OneDrive, Dropbox")
    System_Ext(mail, "E-mail", "Gmail/IMAP")
    System_Ext(cal, "Calendários", "Google/Outlook/CalDAV")
    System_Ext(fin, "Open Finance", "Agregador bancário licenciado")
    System_Ext(push, "Push", "FCM / APNs")
    Rel(user, lumbra, "Conversa, pesquisa, organiza, automatiza")
    Rel(lumbra, ai, "Completions, embeddings, visão, voz (opcional)")
    Rel(lumbra, cloudfs, "Indexa arquivos autorizados")
    Rel(lumbra, mail, "Indexa e-mails autorizados")
    Rel(lumbra, cal, "Lê/escreve eventos")
    Rel(lumbra, fin, "Lê transações (somente leitura)")
    Rel(lumbra, push, "Notificações mobile")
    Rel(lumbra, contact, "Alerta em escalonamento", "opt-in")
```

## Nível 2 — Contêineres

```mermaid
C4Container
    title Lumbra — Contêineres
    Person(user, "Usuário")
    System_Boundary(lumbra, "Lumbra") {
        Container(desktop, "App Desktop", "Electron + Next.js/React/TS", "UI principal; embarca núcleo local")
        Container(mobile, "App Mobile", "Flutter", "Alarmes, captura, chat, widgets")
        Container(web, "App Web", "Next.js", "Acesso via navegador")
        Container(api, "API Gateway", "FastAPI", "REST + WebSocket/SSE, authN/Z")
        Container(core, "Core Intelligence Engine", "Python", "Kernel: memória, RAG, orquestração, automação, plugins, permissões, proatividade")
        Container(workers, "Workers Assíncronos", "Celery", "Indexação, OCR, transcrição, embeddings, automações, consolidação de memória")
        Container(bus, "Event Bus", "Redis Streams", "Pub/sub tipado, DLQ, replay")
        ContainerDb(pg, "PostgreSQL + pgvector", "SQL + vetores", "Dados relacionais, embeddings, knowledge graph, event store")
        ContainerDb(redis, "Redis", "Cache/filas", "Cache, sessões, rate limit, streams")
        ContainerDb(blob, "Object Storage", "S3/MinIO/FS local", "Arquivos e anexos criptografados")
        Container(sync, "Sync Service", "Python", "Reconciliação E2E-criptografada multi-dispositivo")
    }
    Rel(user, desktop, "Usa")
    Rel(user, mobile, "Usa")
    Rel(user, web, "Usa")
    Rel(desktop, api, "HTTPS/WS (modo cloud) ou IPC (modo local)")
    Rel(mobile, api, "HTTPS/WS")
    Rel(web, api, "HTTPS/WS")
    Rel(api, core, "Comandos e consultas")
    Rel(core, bus, "Publica/consome eventos")
    Rel(workers, bus, "Consome eventos")
    Rel(core, pg, "SQLAlchemy")
    Rel(workers, pg, "SQLAlchemy")
    Rel(core, redis, "Cache")
    Rel(workers, blob, "Lê/grava arquivos")
    Rel(sync, pg, "Log de operações")
```

## Nível 3 — Componentes do Core Intelligence Engine

```mermaid
C4Component
    title Core Intelligence Engine — Componentes
    Container_Boundary(core, "Core Intelligence Engine") {
        Component(bus, "Event Bus", "Redis Streams + schemas Pydantic", "Contrato único de comunicação")
        Component(memsys, "Memory System", "Python", "5 memórias + consolidação + esquecimento")
        Component(rag, "RAG Engine", "Python", "Chunking, retrieval híbrido, re-ranking, citação")
        Component(kg, "Knowledge Graph Service", "Python + PG", "Entidades, relações, travessia")
        Component(orch, "Agent Orchestrator", "Python", "Registro, roteamento, supervisão de agentes")
        Component(auto, "Automation Engine", "Python", "DAG de fluxos, retry, DLQ")
        Component(plug, "Plugin Host", "Python/Node sandbox", "Ciclo de vida e permissões de plugins")
        Component(perm, "Permission Manager", "Python", "Consentimento granular, audit log")
        Component(proa, "Proactivity Engine", "Python", "Sinais → regras/IA → sugestões explicáveis")
        Component(ai, "AI Abstraction Layer", "Python", "Router multi-provedor, cache, orçamento")
        Component(idx, "Indexing Pipeline", "Python", "Watchers, extração, OCR, transcrição, embeddings")
    }
    Rel(orch, ai, "complete/stream/embed")
    Rel(rag, ai, "embed/rerank")
    Rel(rag, kg, "expande contexto")
    Rel(memsys, rag, "fornece memórias")
    Rel(idx, memsys, "alimenta memória semântica")
    Rel(proa, perm, "verifica autorização de sinais")
    Rel(plug, perm, "manifesto de permissões")
    Rel(bus, orch, "eventos")
    Rel(bus, auto, "gatilhos")
    Rel(bus, proa, "sinais")
```

## Nível 3 (v2, Etapa 3) — Core Intelligence Engine implementado

Estado real do kernel após a Etapa 3 (`src/lumbra/kernel/`):

```mermaid
C4Component
    title Core Intelligence Engine — componentes implementados
    Container_Boundary(core, "LumbraKernel (composition root)") {
        Component(skills, "SkillRegistry", "kernel/skill_registry.py", "Tool Registry unificado (ADR-015): registro, discovery, execução mediada com permissão + observabilidade")
        Component(ctx, "Context Engine", "kernel/context_engine.py", "Agrega provedores em paralelo: timeout, isolamento de falhas, orçamento, ordenação por relevância")
        Component(plan, "Planner + PlanRunner", "kernel/planning.py", "PlannerPort decompõe objetivos; PlanRunner executa com dependências e tolerância a falha parcial")
        Component(core_mod, "KernelCoreModule", "kernel/core_module.py", "Skills meta: list_capabilities, gather_context")
        Component(life, "Ciclo de vida", "kernel/kernel.py", "Módulos: setup → start (ordem) → stop (inversa); readiness checks")
        Component(pub, "publish()", "kernel/kernel.py", "Ponto único: envelopa → event store (auditoria) → bus")
    }
    Component_Ext(bus, "EventBusPort", "in-memory | Redis Streams")
    Component_Ext(store, "EventStorePort", "in-memory | PG (futuro)")
    Component_Ext(perm, "PermissionPort", "estático | consents em PG (futuro)")
    Component_Ext(prov, "ContextProviderPort", "memória, agenda, docs... (E1+)")
    Rel(skills, perm, "checa escopos")
    Rel(skills, pub, "skill.executed / skill.failed")
    Rel(pub, store, "append (ADR-016)")
    Rel(pub, bus, "publish")
    Rel(ctx, prov, "provide() em paralelo")
    Rel(plan, skills, "descobre e executa")
    Rel(core_mod, skills, "registra skills")
    Rel(life, bus, "start/stop")
```

## Fluxo dos agentes — objetivo complexo (Etapa 3)

```mermaid
sequenceDiagram
    autonumber
    participant U as Usuário/Agente
    participant K as LumbraKernel
    participant CE as Context Engine
    participant PL as Planner (port)
    participant PR as PlanRunner
    participant SR as SkillRegistry
    participant PM as PermissionPort
    participant ES as Event Store
    U->>K: objetivo ("agendar consulta e criar lembrete")
    K->>CE: gather(ContextRequest)
    CE-->>K: fragmentos com proveniência
    K->>PL: plan(goal, skills=catálogo, context)
    PL-->>K: Plan[schedule_event → create_alarm(dep 0)]
    K->>PR: run(plan, SkillContext)
    loop cada passo (dependências respeitadas)
        PR->>SR: execute(skill, input, ctx)
        SR->>PM: is_allowed(subject, scope)
        SR->>SR: valida input (Pydantic)
        SR-->>PR: output tipado
        SR--)ES: skill.executed (auditoria + métrica)
    end
    PR-->>U: PlanResult (completed/failed/skipped por passo)
```

Como isso suporta o futuro: um **novo agente** = um `LumbraModule` que registra skills e consumidores — nada mais muda; um **plugin** = mesmo contrato via Plugin Host (Beta), com manifesto de permissões negado por padrão no `PermissionPort`; uma **nova integração** (agenda, e-mail...) = um `ContextProviderPort` + skills (`schedule_event`, `send_email`) que o Planner passa a usar automaticamente via discovery — nenhum código existente é alterado.


## Pipeline de ingestão (E1 Etapa 2) — componentes e fluxo

```mermaid
flowchart LR
    subgraph Fontes["DataSourcePort (universal)"]
        FS[Filesystem] -.futuro.- GD[Drive/OneDrive/E-mail/WhatsApp...]
    end
    FS --> SK["skill document.index<br/>(dedup por hash + versionamento)"]
    SK -- indexing.file_detected --> BUS[(Event Bus)]
    BUS --> W[ingestion-worker]
    W --> RUN[PipelineRunner<br/>estado persistido + retomada]
    RUN --> S1[extract] & S2[ocr*] --> S3[metadata<br/>plugins] --> S4[chunk<br/>estratégias] --> S5[index<br/>tsv+GIN] --> S6[kg<br/>entidades]
    RUN -. timeline + métricas .-> OBS[(document_timeline / MetricsPort)]
    S6 --> DONE[indexed]
    style S2 stroke-dasharray: 4
```

*ocr: estágio registrado; provider concreto plugável via `OCRProviderPort`.

**Como crescer sem mudanças estruturais:** nova fonte (Drive, e-mail, WhatsApp) = 1 adapter de `DataSourcePort` — mesmo dedup, versões, estados e busca; novo tipo de mídia (áudio/vídeo) = novos estágios (`speech_to_text`, `audio_extract`) registrados no runner + um plano no resolver — nenhuma linha dos existentes muda; novo extrator/chunker/OCR = plugin registrado nas interfaces do ADR-021. O Playground (`/api/v1/dev/playground`, fora de produção, autenticado) inspeciona tudo: estados em tempo real, texto extraído, metadados, entidades, chunks, versões, grafo, busca com explicação de ranking e métricas.
